# Active Context

**Estado:** Implementación inicial completada.

**Foco:** Probar con API key real; completar nombres del grupo en README.

**Próximo paso:** `npm install && npm run dev` o `docker compose up --build`.
